
import edu.sc.seis.launch4j.tasks.Launch4jLibraryTask

apply plugin: 'launch4j'

task buildExe(type: Launch4jLibraryTask) {
    outputDir = 'asdf'
}


launch4jHelper.configure(buildExe) {
    outputDir = 'asdf'
}